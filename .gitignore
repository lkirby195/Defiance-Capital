# Client-supplied, never commit. Anchored to the repository root: api/templates/ holds the
# review queue's own Jinja pages, which are source and are committed.
/templates/
fixtures/historical/

# Secrets and env
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local data
*.db
*.sqlite

# Committed template for .env
!.env.example
